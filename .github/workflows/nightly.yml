name: nightly

on:
  schedule:
    - cron: "0 1 * * *"
# on:
#   push:
#     branches:
#       - master
#       - main

env:
  UPLOAD_BUILD: false

jobs:
  QIO:
    strategy:
      fail-fast: false
      matrix:
        example:
          - "arduino-blink"
          - "arduino-external-libs"
          - "arduino-internal-libs"
          - "arduino-mbed-doom"
          - "arduino-mbed-rpc"
          - "arduino-mxchip-azureiot"
          - "arduino-mxchip-filesystem"
          - "arduino-mxchip-sensors"
          - "arduino-mxchip-wifiscan"
          - "cmsis-blink"
          - "mbed-rtos-blink-baremetal"
          - "mbed-rtos-custom-target"
          - "mbed-rtos-ethernet-tls"
          - "mbed-rtos-events"
          - "mbed-rtos-filesystem"
          - "mbed-rtos-mesh-minimal"
          - "mbed-rtos-serial"
          - "mbed-rtos-usb-keyboard"
          - "mbed-rtos-wifi"
          - "mbed-blink"
          - "mbed-custom-target"
          - "mbed-dsp"
          - "mbed-events"
          - "mbed-filesystem"
          - "mbed-rtos"
          - "mbed-rtos-ethernet-tls"
          - "mbed-rtos-mesh-minimal"
          - "mbed-serial"
          - "spl-blink"
          - "stm32cube-hal-blink"
          - "stm32cube-hal-eeprom-emulation"
          - "stm32cube-hal-extmem-boot"
          - "stm32cube-hal-iap"
          - "stm32cube-hal-lcd"
          - "stm32cube-hal-usb-device-dfu"
          - "stm32cube-hal-wifi-client"
          - "stm32cube-ll-blink"

        os: [ubuntu-latest, windows-latest, macos-latest]
        package:
          - "all"
          - "qio"
          - "pio"

    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: "recursive"
          fetch-depth: 1

      - name: Python
        uses: actions/setup-python@v1
        with:
          python-version: 3.9

      - name: Install all
        if: matrix.package == 'all'
        run: |
          python -m pip install --upgrade pip
          pip install -U https://github.com/OS-Q/M03/releases/latest/download/qio.zip
          pio platform install https://github.com/OS-Q/P21/releases/latest/download/package.zip

      - name: Install qio
        if: matrix.package == 'qio'
        run: |
          python -m pip install --upgrade pip
          pip install -U https://github.com/OS-Q/M03/releases/latest/download/qio.zip
          qio platform install https://github.com/OS-Q/P21/releases/latest/download/qio.zip

      - name: Install pio
        if: matrix.package == 'pio'
        run: |
          python -m pip install --upgrade pip
          pip install -U https://github.com/OS-Q/M03/releases/latest/download/qio.zip
          qio platform install https://github.com/OS-Q/P21/releases/latest/download/pio.zip

      - name: Build
        if: success()
        working-directory: examples
        run: |
          pio run -d ${{ matrix.example }}

      - name: Upload
        if: env.UPLOAD_BUILD
        uses: actions/upload-artifact@v2
        with:
          name: build_${{ matrix.example }}_${{ matrix.package }}
          path: examples/${{ matrix.example }}/.pio/build/

  QIO_fail:
    needs:
      - QIO
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: notification
        uses: Qful/mqtt-action@master
        with:
          protocol: mqtt
          host: ${{secrets.MQTT_SERVER}}
          port: 1883
          username: ${{secrets.MQTT_USERNAME}}
          password: ${{secrets.MQTT_PASSWORD}}
          topic: ${{github.repository}}/${{github.workflow}}
          message: "QIO build failure"

  PIO:
    strategy:
      fail-fast: false
      matrix:
        example:
          - "arduino-blink"
          - "arduino-external-libs"
          - "arduino-internal-libs"
          - "arduino-mbed-doom"
          - "arduino-mbed-rpc"
          - "arduino-mxchip-azureiot"
          - "arduino-mxchip-filesystem"
          - "arduino-mxchip-sensors"
          - "arduino-mxchip-wifiscan"
          - "cmsis-blink"
          - "mbed-rtos-blink-baremetal"
          - "mbed-rtos-custom-target"
          - "mbed-rtos-ethernet-tls"
          - "mbed-rtos-events"
          - "mbed-rtos-filesystem"
          - "mbed-rtos-mesh-minimal"
          - "mbed-rtos-serial"
          - "mbed-rtos-usb-keyboard"
          - "mbed-rtos-wifi"
          - "mbed-blink"
          - "mbed-custom-target"
          - "mbed-dsp"
          - "mbed-events"
          - "mbed-filesystem"
          - "mbed-rtos"
          - "mbed-rtos-ethernet-tls"
          - "mbed-rtos-mesh-minimal"
          - "mbed-serial"
          - "spl-blink"
          - "stm32cube-hal-blink"
          - "stm32cube-hal-eeprom-emulation"
          - "stm32cube-hal-extmem-boot"
          - "stm32cube-hal-iap"
          - "stm32cube-hal-lcd"
          - "stm32cube-hal-usb-device-dfu"
          - "stm32cube-hal-wifi-client"
          - "stm32cube-ll-blink"

        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v2
        with:
          submodules: "recursive"
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v1
        with:
          python-version: 3.9


      - name: Install platformio
        run: |
          python -m pip install --upgrade pip
          pip install -U https://github.com/platformio/platformio/archive/develop.zip
          pio platform install https://github.com/OS-Q/P21/releases/latest/download/package.zip

      - name: Build
        if: success()
        working-directory: examples
        run: |
          pio run -d ${{ matrix.example }}

      - name: Upload
        if: env.UPLOAD_BUILD
        uses: actions/upload-artifact@v2
        with:
          name: pio_build_${{ matrix.example }}
          path: examples/${{ matrix.example }}/.pio/build/

  PIO_fail:
    needs:
      - PIO
    if: failure()
    runs-on: ubuntu-latest
    steps:
      - name: notification
        uses: Qful/mqtt-action@master
        with:
          protocol: mqtt
          host: ${{secrets.MQTT_SERVER}}
          port: 1883
          username: ${{secrets.MQTT_USERNAME}}
          password: ${{secrets.MQTT_PASSWORD}}
          topic: ${{github.repository}}/${{github.workflow}}
          message: "PIO build failure"
